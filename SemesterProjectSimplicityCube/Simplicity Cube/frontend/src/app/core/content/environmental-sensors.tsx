import React from 'react';
import { css, cx } from '@emotion/css';

interface Props {}

export const EnvironmentalSensors = (props: Props) => {
  const styles = getStyles();

  return <div></div>;
};

const getStyles = () => {
  return {
    dummy: css``,
  };
};
